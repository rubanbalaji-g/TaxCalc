# Publish directory (root of the repo)
publish = "."

# No build command needed – pure static HTML/CSS/JS
[build]
  command = ""
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
